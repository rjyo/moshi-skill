interface:
  display_name: "Moshi Best Practices"
  short_description: "Prepare or verify a host for Moshi."
  default_prompt: "Use $moshi-best-practices to prepare or verify a host for Moshi, configure tmux, create a tmux project session, optionally install the `moshi DIR` shell helper, and set up moshi-hook."
